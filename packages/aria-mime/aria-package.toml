[package]
name = "aria-mime"
version = "0.2.6"
description = "MIME type detection from file extensions for Aria"
license = "MIT"
repository = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_mime_shim"]
