[package]
name            = "aria-smtp"
version         = "0.3.1"
description     = "SMTP email client for Aria — compose and send emails via SMTP servers"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_smtp_shim"]
