[package]
name            = "aria-postgres"
version         = "0.2.3"
description     = "PostgreSQL database client for Aria — connect, query, parameterized queries, transactions via libpq"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_postgres_shim", "pq"]
