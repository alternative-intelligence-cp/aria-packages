[package]
name            = "aria-websocket"
version         = "0.2.6"
description     = "WebSocket client and server for Aria — connect, send, receive, broadcast via libwebsockets"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria-packages"
keywords        = ["websocket", "networking", "realtime", "ws"]
categories      = ["networking", "web"]

[dependencies]

[build]
link_libraries = ["aria_websocket_shim", "websockets"]
