[package]
name            = "aria-dns"
version         = "0.3.1"
description     = "DNS resolution for Aria — A/AAAA hostname lookups via system resolver"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_dns_shim"]
