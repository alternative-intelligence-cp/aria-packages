[package]
name = "aria-rules-common"
version = "0.3.0"
authors = ["Aria Language Project"]
license = "MIT"
description = "Commonly used Rules<T> declarations — numeric ranges, string validation, null/NIL safety, array constraints, multi-type bounds"
keywords = ["rules", "validation", "constraints", "safety", "refinement-types"]
categories = ["safety", "utility"]

[dependencies]

[build]
type = "library"
entry = "src/aria_rules_common.aria"
