[package]
name = "aria-stats"
version = "0.11.1"
description = "Descriptive statistics — mean, median, mode, stddev, percentile, correlation"
entry = "src/aria_stats.aria"
type = "aria-libc"
deps = ["aria-libc_mem", "aria-libc_math"]
