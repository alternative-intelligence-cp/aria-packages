[package]
name = "aria-static"
version = "0.12.1"
description = "Static file path resolution and MIME type detection for Aria — pure Aria"
license = "MIT"
categories = ["networking", "middleware"]

[dependencies]
