[package]
name = "aria-crypto"
version = "0.2.6"
description = "Cryptographic hashing (SHA-256, MD5, HMAC) for Aria"
license = "MIT"
repository = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_crypto_shim"]
