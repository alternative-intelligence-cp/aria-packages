[package]
name            = "aria-mysql"
version         = "0.2.3"
description     = "MySQL database client for Aria — connect, query, parameterized queries, transactions via libmysqlclient"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_mysql_shim", "mysqlclient"]
