[package]
name = "aria-semver"
version = "0.2.6"
description = "Semantic versioning (SemVer) parsing and comparison for Aria"
license = "MIT"
repository = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_semver_shim"]
