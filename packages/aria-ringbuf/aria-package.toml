[package]
name            = "aria-ringbuf"
version         = "0.3.0"
description     = "Fixed-size ring buffer (circular buffer) for Aria — handle-based, int64 elements"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_ringbuf_shim"]
