[package]
name            = "aria-pqueue"
version         = "0.3.0"
description     = "Priority queue for Aria — min-heap based, handle-based, int64 elements with int32 priorities"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/aria-packages"

[dependencies]

[build]
link_libraries = ["aria_pqueue_shim"]
