[package]
name = "aria-cli"
version = "0.11.2"
description = "Rich CLI output with ANSI colors, styles, and progress bars for Aria"
license = "MIT"
repository = "https://github.com/alternative-intelligence-cp/aria-packages"
categories = ["cli", "terminal"]

[dependencies]

[build]
type = "library"
entry = "src/aria_cli.aria"
link_libraries = ["aria_cli_shim"]
